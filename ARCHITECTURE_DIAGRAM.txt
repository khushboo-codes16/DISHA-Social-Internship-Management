================================================================================
                    REAL-TIME PROGRAMS UPDATE ARCHITECTURE
================================================================================

┌─────────────────────────────────────────────────────────────────────────────┐
│                           STUDENT SIDE                                      │
└─────────────────────────────────────────────────────────────────────────────┘

    Student Dashboard
         │
         │ Creates Program
         ▼
    ┌──────────────────────┐
    │  CreateProgramForm   │
    │  - Title             │
    │  - Type              │
    │  - Location          │
    │  - Date              │
    └──────────┬───────────┘
               │
               │ Submit
               ▼
    ┌──────────────────────┐
    │ student.py           │
    │ create_program()     │
    │                      │
    │ program_data = {     │
    │   'toli_id': ...,    │ ← IMPORTANT: Links to toli
    │   'student_id': ..., │
    │   'title': ...,      │
    │   ...                │
    │ }                    │
    └──────────┬───────────┘
               │
               │ Save
               ▼
    ┌──────────────────────┐
    │   MongoDB Atlas      │
    │   Collection:        │
    │   'programs'         │
    │                      │
    │   {                  │
    │     _id: "...",      │
    │     toli_id: "123",  │ ← Stored with toli_id
    │     title: "...",    │
    │     ...              │
    │   }                  │
    └──────────────────────┘

================================================================================

┌─────────────────────────────────────────────────────────────────────────────┐
│                           ADMIN SIDE                                        │
└─────────────────────────────────────────────────────────────────────────────┘

    Admin Dashboard
         │
         │ Opens Manage Toli
         ▼
    ┌──────────────────────────────────────────────────────────────┐
    │  manage_toli.html                                            │
    │                                                              │
    │  ┌────────────────────────────────────────────────────┐    │
    │  │  Recent Programs Section                           │    │
    │  │                                                     │    │
    │  │  [🔄 Refresh Button]                               │    │
    │  │                                                     │    │
    │  │  <div id="programsList">                           │    │
    │  │    Program 1: Yagya at Delhi                       │    │
    │  │    Program 2: Yoga at Mumbai                       │    │
    │  │    ...                                              │    │
    │  │  </div>                                             │    │
    │  └────────────────────────────────────────────────────┘    │
    │                                                              │
    │  JavaScript:                                                 │
    │  ┌────────────────────────────────────────────────────┐    │
    │  │  async function refreshPrograms() {                │    │
    │  │    // Call API                                     │    │
    │  │    const response = await fetch(                   │    │
    │  │      '/admin/toli/123/programs'                    │    │
    │  │    );                                               │    │
    │  │    const data = await response.json();             │    │
    │  │    // Update UI                                    │    │
    │  │    updateProgramsList(data.programs);              │    │
    │  │  }                                                  │    │
    │  │                                                     │    │
    │  │  // Auto-refresh every 30 seconds                  │    │
    │  │  setInterval(refreshPrograms, 30000);              │    │
    │  └────────────────────────────────────────────────────┘    │
    └──────────────────┬───────────────────────────────────────────┘
                       │
                       │ API Call (every 30s)
                       ▼
    ┌──────────────────────────────────────────────────────────────┐
    │  admin.py - API Endpoint                                     │
    │                                                              │
    │  @admin.route('/admin/toli/<toli_id>/programs')             │
    │  def get_toli_programs(toli_id):                            │
    │                                                              │
    │    # Query database                                         │
    │    programs_data = db.get_programs_by_toli(toli_id)        │
    │                                                              │
    │    # Format response                                        │
    │    programs_list = []                                       │
    │    for program in programs_data:                            │
    │      programs_list.append({                                 │
    │        'id': program.id,                                    │
    │        'title': program.title,                              │
    │        'program_type': program.program_type,                │
    │        ...                                                   │
    │      })                                                      │
    │                                                              │
    │    return jsonify({                                         │
    │      'success': True,                                       │
    │      'programs': programs_list,                             │
    │      'count': len(programs_list)                            │
    │    })                                                        │
    └──────────────────┬───────────────────────────────────────────┘
                       │
                       │ Query
                       ▼
    ┌──────────────────────────────────────────────────────────────┐
    │  database.py                                                 │
    │                                                              │
    │  def get_programs_by_toli(self, toli_id):                   │
    │    return list(                                             │
    │      self.db.programs.find({'toli_id': toli_id})           │
    │    )                                                         │
    └──────────────────┬───────────────────────────────────────────┘
                       │
                       │ Query MongoDB
                       ▼
    ┌──────────────────────────────────────────────────────────────┐
    │   MongoDB Atlas                                              │
    │                                                              │
    │   db.programs.find({'toli_id': '123'})                      │
    │                                                              │
    │   Returns: [                                                │
    │     {_id: "1", toli_id: "123", title: "Yagya", ...},       │
    │     {_id: "2", toli_id: "123", title: "Yoga", ...},        │
    │     ...                                                      │
    │   ]                                                          │
    └──────────────────┬───────────────────────────────────────────┘
                       │
                       │ Return Results
                       ▼
    ┌──────────────────────────────────────────────────────────────┐
    │  API Response (JSON)                                         │
    │                                                              │
    │  {                                                           │
    │    "success": true,                                         │
    │    "programs": [                                            │
    │      {                                                       │
    │        "id": "1",                                           │
    │        "title": "Yagya Program",                            │
    │        "program_type": "Yagya",                             │
    │        "location": "Delhi, Delhi",                          │
    │        "start_date": "19 Nov 2024",                         │
    │        "status": "pending"                                  │
    │      },                                                      │
    │      ...                                                     │
    │    ],                                                        │
    │    "count": 5                                               │
    │  }                                                           │
    └──────────────────┬───────────────────────────────────────────┘
                       │
                       │ Response
                       ▼
    ┌──────────────────────────────────────────────────────────────┐
    │  JavaScript Updates UI                                       │
    │                                                              │
    │  programsList.innerHTML = data.programs.map(program => {    │
    │    return `                                                  │
    │      <div class="program-card">                             │
    │        <h4>${program.title}</h4>                            │
    │        <p>${program.program_type} • ${program.location}</p> │
    │        <span class="status">${program.status}</span>        │
    │      </div>                                                  │
    │    `;                                                        │
    │  }).join('');                                                │
    │                                                              │
    │  // Visual feedback                                         │
    │  programsList.classList.add('bg-green-50');                 │
    │  setTimeout(() => {                                         │
    │    programsList.classList.remove('bg-green-50');            │
    │  }, 1000);                                                   │
    └──────────────────────────────────────────────────────────────┘

================================================================================

┌─────────────────────────────────────────────────────────────────────────────┐
│                           TIMING DIAGRAM                                    │
└─────────────────────────────────────────────────────────────────────────────┘

Time    Student Side              MongoDB              Admin Side
────────────────────────────────────────────────────────────────────────────

00:00   Page loads                                     Page loads
        │                                              │
        │                                              ├─ Initial load
        │                                              ├─ Shows existing programs
        │                                              │
00:05   Creates program                                │
        │                                              │
        ├─ Submit form ──────────►                    │
        │                         Save to DB           │
        │                         (with toli_id)       │
        │                                              │
00:30   │                                              Auto-refresh triggered
        │                                              │
        │                         ◄──── Query DB ─────┤
        │                         Return new data ────►│
        │                                              │
        │                                              ├─ Update UI
        │                                              ├─ Show new program ✅
        │                                              ├─ Green flash animation
        │                                              │
01:00   │                                              Auto-refresh again
        │                         ◄──── Query DB ─────┤
        │                         Return data ────────►│
        │                                              │
        │                                              Update if changed
        │                                              │

Manual Refresh:
        │                                              User clicks 🔄
        │                         ◄──── Query DB ─────┤
        │                         Return data ────────►│
        │                                              Instant update ✅

================================================================================

┌─────────────────────────────────────────────────────────────────────────────┐
│                           KEY COMPONENTS                                    │
└─────────────────────────────────────────────────────────────────────────────┘

1. STUDENT CREATES PROGRAM
   File: app/routes/student.py
   Function: create_program()
   Key: Saves toli_id with program

2. MONGODB STORAGE
   Collection: programs
   Key Field: toli_id (links program to toli)

3. API ENDPOINT
   File: app/routes/admin.py
   Route: /admin/toli/<toli_id>/programs
   Purpose: Fetch fresh programs

4. DATABASE QUERY
   File: app/database.py
   Function: get_programs_by_toli(toli_id)
   Query: db.programs.find({'toli_id': toli_id})

5. FRONTEND UPDATE
   File: app/templates/admin/manage_toli.html
   Function: refreshPrograms()
   Frequency: Every 30 seconds + manual

================================================================================

┌─────────────────────────────────────────────────────────────────────────────┐
│                           DATA FLOW SUMMARY                                 │
└─────────────────────────────────────────────────────────────────────────────┘

Student → Form → Save (with toli_id) → MongoDB
                                          ↓
                                    Stored with toli_id
                                          ↓
Admin → JavaScript → API Call → Query DB → Return JSON → Update UI
        (every 30s)                ↑
                                   │
                            No Cache - Always Fresh!

================================================================================

RESULT: Real-time synchronization between student program creation and 
        admin dashboard display! ✅

================================================================================
